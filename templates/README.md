# Templates

This directory holds install-time source material for LCLP.

Current contents:

- `workflow/`: the runtime skeleton template copied into target repositories as
  `workflow/`

The source repository keeps template material here so it is visibly distinct
from a live installed `workflow/` tree.
